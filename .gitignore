# Build artifacts (generated by build.js from registry.json — both the
# CF Pages integration build and the GH Actions deploy regenerate this
# tree on every deploy, so it must not live in git or it drifts.)
dist/

node_modules/
.wrangler/
wrangler.toml
.gstack/
